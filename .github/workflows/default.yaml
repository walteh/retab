# code generated by retab. DO NOT EDIT.
# join the fight against yaml @ github.com/walteh/retab

# source: "tmp.yaml.retab"

name: test
"on":
    push:
        branches:
            - main
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout
              uses: actions/checkout@v2
              withd:
                fetch-depth: 3
            - name: Run tests
              run: |
                echo "Hello world"
