# code generated by retab (devel). DO NOT EDIT.
# join the fight against yaml @ github.com/walteh/retab

# source: ".retab/task.retab"

version: 3
tasks:
    generate:
        cmd: mockery
        generates:
          - ./gen/mockery/*.go
        sources:
          - ./**/*.go

    test:
        cmd: go test ./...

    rebuild-extension:
        dir: code-extension
        cmds:
          - rm -rf node_modules
          - rm -rf out
          - mkdir -p out
          - cp "$(go env GOROOT)/lib/wasm/wasm_exec.js" out/
          - cd ../cmd/retab-wasm && GOOS=js GOARCH=wasm go build -o ../../code-extension/out/retab.wasm
          - bun install
          - bun run package
          - mv ./retab-*.vsix ~/Downloads
          - open https://marketplace.visualstudio.com/manage/publishers/walteh
