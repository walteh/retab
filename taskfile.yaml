# code generated by retab (devel). DO NOT EDIT.
# join the fight against yaml @ github.com/walteh/retab

# source: ".retab/task.retab"

version: 3
tasks:
    generate:
        cmd: mockery
        generates:
            - ./gen/mockery/*.go
        sources:
            - ./**/*.go

    test:
        cmd: go test ./...

    vscode:refresh-go:
        dir: editors/vscode
        cmds:
            - rm -rf out
            - mkdir -p out
            - cp "$(go env GOROOT)/lib/wasm/wasm_exec.js" out/
            - cd ../cmd/retab && GOOS=js GOARCH=wasm go build -o ../../editors/vscode/out/retab.wasm
            - bun install

    vscode:refresh-tinygo:
        dir: editors/vscode
        cmds:
            - rm -rf out
            - mkdir -p out
            - cp "$(tinygo env TINYGOROOT)/targets/wasm_exec.js" out/wasm_exec.tinygo.js
            - cd ../cmd/retab && GOOS=js GOARCH=wasm tinygo build -o ../../editors/vscode/out/retab.wasm
            - bun install

    vscode:package:
        dir: editors/vscode
        cmds:
            - task: vscode:refresh-go
            - bun run package
            - mv ./retab-*.vsix ~/Downloads
            - open https://marketplace.visualstudio.com/manage/publishers/walteh

    vscode:rebuild:
        dir: editors/vscode
        cmds:
            - task: vscode:refresh-go
            - bun run build
